server {
    listen 80;
    server_name _;

    location / {
        auth_basic "Brutus Mini Lab";
        auth_basic_user_file /etc/nginx/.htpasswd;
        root /usr/share/nginx/html;
        index index.html;
    }
}

